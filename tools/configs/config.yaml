# default yaml
#

setup: simple_diffusion
run_idx: -1
debug: False
defaults:
    - _self_
    - base: base
    - datasets: cityscapes
    - env: root_paths
    - distributed: local

hydra:  
  output_subdir: null
base: base

env: root_paths
distributed: local